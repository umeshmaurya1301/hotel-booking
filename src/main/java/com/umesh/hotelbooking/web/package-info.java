/**
 * Cross-cutting API-layer plumbing: correlation, request context, api-type derivation and the
 * (stubbed) role check — everything that makes the existing controllers a uniform surface
 * rather than a business capability of its own (design doc 11).
 */
package com.umesh.hotelbooking.web;
